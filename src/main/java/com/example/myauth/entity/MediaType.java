package com.example.myauth.entity;

/**
 * 미디어 유형 Enum
 * 게시글에 첨부된 미디어의 유형을 구분
 */
public enum MediaType {
  /**
   * 이미지 파일 (JPEG, PNG, GIF, WEBP 등)
   */
  IMAGE,

  /**
   * 동영상 파일 (MP4, MOV 등)
   */
  VIDEO
}